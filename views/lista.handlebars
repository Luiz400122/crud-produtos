
    <!--TABELA para listagem-->

    <!--Exibir mensagem de produto não encontrado -->

    {{#if semRegistro}}
        <h3 class="text text-center text-danger">Nenhum produto encontrado :( </h3>

    {{/if}}

    <div class="container">
        <div class="row">
           {{#each produtos}}
            <div class="col-12 col-sm-6 col-md-4 col-lg-3">
                <div class="card">
                    <img src="../imagens/{{imagem}}">
                    <div class="text">
                         <p>{{nome}}</p>
                        <p>R$ {{valor}}</p>
                     </div>
                     <div class="btcontainer">
                         <a href="/editar/{{codigo}}" class="btn btn-warning btn-sm">Alterar</a>
                        <a href="/deletar/{{codigo}}&{{imagem}}" type="submit" id="btnremove" class="btn btn-danger btn-sm">Remover</a>
                     </div>
                       
                </div>
            </div>
           {{/each}}
        </div>
    </div>
</main>

<script>

</script>